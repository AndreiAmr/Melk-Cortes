import styled from "styled-components";

export const Container = styled.div`
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 100vh;
    background: var(--white);
    z-index: 999999;
    padding: 40px;

    svg{
        width: 80%;
    }
`
